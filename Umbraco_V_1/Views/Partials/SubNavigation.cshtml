@inherits Umbraco.Web.Mvc.UmbracoTemplatePage

@if (CurrentPage.DocumentTypeAlias == "topLevelPage")
{
    <div class="col-lg-12">

        @foreach (var childPage in CurrentPage.Children().Where("Visible"))
        {
            <div class="common">
                <a href="@childPage.Url">@childPage.Name</a>
            </div>
        }
    </div>
	@*<div style="space">&nbsp</div>*@
		
    @*@<ul>

            @foreach (var childPage in CurrentPage.Children().Where("Visible"))
            {
                <li>
                    <a href="@childPage.Url">@childPage.Name</a>
                </li>
            }
        </ul>*@
}
@if (CurrentPage.Parent != null && CurrentPage.Parent.DocumentTypeAlias == "topLevelPage")
{
    <div class="col-lg-12" style="padding-bottom=32px">

		@foreach (var childPage in CurrentPage.Parent.Children().Where("Visible"))
    	{
            <div class="common">
                <a href="@childPage.Url">@childPage.Name</a>
            </div>
        }
    </div>

    @*<ul>

        {
            <li>
                <a href="@childPage.Url">@childPage.Name</a>
            </li>
        }
    </ul>*@
}

